# Server Configuration
server.port=${PORT:8080}
spring.application.name=CMFI-Rwanda-Church

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cmci.rwanda@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# CORS Configuration - Use allowedOriginPatterns instead of allowedOrigins when using credentials
cors.allowed.origins=http://localhost:5173,http://localhost:3000,https://cmfi-website.vercel.app